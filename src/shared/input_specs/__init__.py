"""Shared input-spec library: readouts and encoder specs.

Package-independent of ``r2dreamer`` / ``vggt``. Construct readouts from
``main`` via the exported classes (and later ``get_spec`` / ``ENCODER_SPECS``).
"""
