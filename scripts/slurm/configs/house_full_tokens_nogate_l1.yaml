extends: house_context_l1
job_name: vggt-house-full-tokens-nogate-l1
output_dir: output/runs/r2dreamer-curriculum-l1-vggt-house-full-tokens-nogate
run_id: habitat-l1-vggt-house-full-tokens-nogate
comments:
  - "3D-77 no-gate full-token test: replay stores RGB_64 only; live bounded InfiniteVGGT emits 1374x2048 tokens; trainable agent encoder runs CNN(image)+Transformer(tokens) and concatenates without WP/CP MLP or learned scalar gate"
args:
  output_dir: output/runs/r2dreamer-curriculum-l1-vggt-house-full-tokens-nogate/run-${SLURM_JOB_ID}
  wandb_name: l1_rgb_replay_vggt_house_full_tokens_nogate-${SLURM_JOB_ID}
  wandb_tags: curriculum,level1,1house,chair-only,vggt,house-context,full-token-transformer,no-gate,rgb-replay,live-cache,bounded-cache,3d-77,jax,3d-encoder
smoke:
  time: "00:30:00"
  args:
    steps: 800
    prefill: 200
    batch_size: 4
    seq_len: 16
    train_ratio: 16
    log_every: 50
    checkpoint_every: 10000
    output_dir: output/smoke/vggt-house-full-tokens-nogate-${TIMESTAMP}
    wandb_name: vggt-house-full-tokens-nogate-smoke-${TIMESTAMP}
    wandb_tags: house-context,smoke,full-token-transformer,no-gate,rgb-replay,live-cache
