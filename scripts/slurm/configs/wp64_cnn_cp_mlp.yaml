extends: _base
job_name: wp64-cnn-cp-mlp
output_dir: output/r2dreamer-curriculum-l1-vggt-wp64-cnn-cp-mlp
run_id: habitat-l1-vggt-wp64-cnn-cp-mlp
curriculum_check: data/curriculum/level1_1house_1goal.json
comments:
  - "3D-89 hypothesis: 64x64x3 VGGT world points through ConvEncoder(input_kind=world_points) + 9-D camera pose MLP."
  - "Replay stores separate float16 fields: world_points (3,64,64) and camera_pose (9,)."
  - "Compares against wp_cp_mlp_64, which flattens the same WP+CP information into one MLP input."
sbatch:
  mem: 96G
args:
  steps: 2000000
  prefill: 5000
  checkpoint_every: 100000
  output_dir: output/r2dreamer-curriculum-l1-vggt-wp64-cnn-cp-mlp/run-${SLURM_JOB_ID}
  seed: ${SLURM_JOB_ID}
  log_every: 250
  wandb_project: 3d-vla-objectnav
  wandb_name: wp64-cnn-cp-mlp-${SLURM_JOB_ID}
  wandb_tags: curriculum,level1,1house,chair-only,vggt,wp-64,wp-cnn,cp-mlp,3d-89,jax
  render_resolution: 518
