# investigate-vggt-scene-memory

Characterize how VGGT's per-frame point-cloud output accumulates over time (1 image / 1 episode / multi-episode), then compare external scene-memory mechanisms (voxel hash table vs graph memory) only where VGGT's internal KV-cache memory stops sufficing.
