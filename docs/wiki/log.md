# Wiki Log

## [2026-05-04] decide | Keep Option 1 (`--jax-static-budgets` override) over Option 3 | source: side-by-side bench
Ran the head-to-head PT-bf16 vs JAX-static-budgets bench on H100 at n=10/50/100. Option 1 medians: 68.9 / 76.1 / **76.1 ms**. PT medians: 67.5 / 94.9 / 160.8 ms. Option 1 is 2.1× faster than PT at n=100 *and* has flat per-frame latency post-warmup (the InfiniteVGGT "constant cost" property only the padded-shape JAX path actually delivers). Option 3 (`worktree-agent-a17820b10c561756e`, runtime budget arg + masked top_k) preserves PT-bit-parity but lands at 132/145/170 ms — *slower than PT* at n=100. Decision: **keep Option 1** for the #98 uniformity-not-speed track + WM-in-JAX motivation. Option 3 stays in the worktree as future optionality if PT bit-parity ever becomes load-bearing. Updated `methods/vggt-jax-eviction-recompile.md` with the comparison table and revised recommendation. CSVs: `output/methods/vggt-jax-latency/vggt_streaming_20260504_101104.csv` + `_095744.csv`.

## [2026-05-04] verify | JAX VGGT Option 3 (runtime budget arg + masked top_k) | source: sub-agent worktree `agent-a17820b10c561756e`
Ran the full integration + parity test matrix against the agent's worktree using main's venv (`.venv/bin/pytest` + `PYTHONPATH=<worktree>`). Result: **36/36 substantive tests pass**, 1 environmental ERROR (`torch.OutOfMemoryError` after 35 prior tests filled 92/93 GiB GPU memory — not a regression). Critical diagnostic tests pass: `test_no_post_eviction_recompile_cliff` (locks the fix in), `test_dynamic_budget_formula_matches_pytorch`, `test_eviction_fires_at_same_frame`, `test_retained_kv_matches_pytorch`, `test_per_frame_output_matches_pytorch` (×2 classes), `test_anchor_kv_matches_pytorch`, full PT↔JAX cache parity end-to-end. Option 3's `top_k(k=MAX-anchor) + runtime mask` pattern preserves the dynamic-budget formula bit-exactly while making the per-frame budget a runtime jnp.int32 array. Updated `methods/vggt-jax-eviction-recompile.md` with the verification table and merge recommendation: **replace the `--jax-static-budgets` override (Option 1, currently in main) with Option 3**, drop the override CLI / ctor kwarg.

## [2026-05-04] implement | torch.compile mode sweep (PT VGGT path) | source: l4-profiling.md "Update 2026-05-04"
Extended `VGGTFeatureExtractor` with `compile_mode: str | None = None`; plumbed `--compile-mode` through `profile_training.py` and `--pt-compile-mode` through `benchmark_streaming.py`. Ran four separate `uv run` H100 invocations (no-compile, default, reduce-overhead, max-autotune) under `output/methods/vggt-jax-latency/compile-modes/`. n=100 medians: no-compile **160.6 ms**, default **144.8 ms** (−9.8%), reduce-overhead and max-autotune **both FAILED** with `RuntimeError: accessing tensor output of CUDAGraphs that has been overwritten by a subsequent run` (camera_head.py:94). Both CUDA-graph modes are incompatible with the InfiniteVGGT streaming KV-cache aliasing pattern; fixing it requires a `clone()` or `cudagraph_mark_step_begin()` insertion inside `extract()` — a method change outside this lever. **Winner by elimination: `default`**. L4 production sbatch recommendation: keep `--compile`, do not pass `--compile-mode` (None == default). Stack progress: ~10% on the bottleneck phase is insufficient to hit the ~4 d → ~3 d target alone — remaining gap must come from FastVGGT / distillation / async pipeline.

## [2026-05-04] diagnose | JAX VGGT eviction-onset recompile cliff | source: /diagnose run on `benchmark_streaming.py`
Re-ran the streaming bench under current main. n=10/50/100 medians: PT 65.8 / 96.8 / 162.4 ms; JAX 64.9 / 76.2 / **24_511** ms. Per-frame probe (`output/methods/vggt-jax-latency/probe.csv`) localised the cliff to frames 36→37: 109 ms (eviction first fires) → **43_125 ms** (XLA recompile), and every subsequent frame keeps recompiling. Root cause: `feature_extractor.py:354` passes `budgets_static` (tuple of int32 from `softmax(diversity)*total_budget`) as `static_argnums=7` of `_aggregator_apply`. Pre-eviction `last_scores=zeros` → fixed tuple → cached compile. Post-eviction `last_scores` drifts continuously → tuple changes every frame → static-arg miss → fresh compile each frame. Updated `benchmark_streaming.py` to honestly report `config` (was claiming "JAX eager fp32" while code defaults to bf16-jitted). Created `methods/vggt-jax-eviction-recompile.md`. Implication for #98: "within 1.5× of PT-compile" parity threshold was set on pre-cap data; post-cap JAX is >250× slower until this is fixed. Three fix options ranked in the wiki page (uniform budget / quantize / runtime budget arg).

## [2026-05-02] implement | Encoder Fusion blockers B1/B2/B4 — first pass | source: PR #110 follow-up
Three blockers from the encoder-fusion audit progressed: (B1) wrote design proposal at methods/pose-as-jax-leaf.md spelling out the minimal API change to make `pose` a separable JAX leaf in `_loss_fn` — ~100 LOC across 3 files, agent-side `_split_obs_pose` slicing keeps replay buffer untouched. (B2) extended modules/envs/habitat_r2dreamer.py to surface `agent_state` (4×4 cam→world extrinsics + 3×3 K intrinsics, packed as 25-D float32) in obs dict via pure helper `build_agent_state(pos, quat_xyzw, hfov_deg, H, W)`; 7 unit tests pass without habitat-sim. (B4) ran scripts/verify_aggregated_tokens.py on dev_gpu_h100 — `aggregated_tokens` is a list of 24 layer tensors each `(B, S, 1374, 2048)` with `patch_start_idx=5`; channel is 2048 not 1024 as assumed, so the fixed projection is 2048→K. Updated encoder-fusion-plan.md Step 0 to mark B4 resolved with the corrected layout. B3 (buffer decision) and the actual B1 code restructure remain open.

## [2026-05-02] revise | Encoder Fusion Plan — apply audit findings | source: PR #110 follow-up
Revised methods/encoder-fusion-plan.md to address all 5 blockers + 3 major risks from the audit. Added Phase 0 (baseline variance calibration before the matrix runs). Added `vggt_pose_scaled` (issue #87 Trivial fix) as a 5th encoder variant + 3 SLURM jobs, expanding the matrix from 12 to 15 runs. Promoted Habitat-wrapper extension into Phase 1 (Plücker variant is blocked-by-default without it). Corrected buffer math: 22× growth = 367 GB (not 350), with explicit options (cap/disk-back/compress) the team must pick before Phase 1. Patched 4 wrong cited paths (registries.py:15, agent.py:299, evaluate.py:72, scripts/slurm/). Added JL-bound caveats: 64 dims is two orders below the bound; matrix must be seeded + serialized. Made Phase 5 monotonicity rule quantitative (t-test on pose_grad_norm). Added MANIFEST + reporter/`_blessed/` discipline to Phase 4.

## [2026-05-02] audit | Encoder Fusion Plan (PR #110) — 3-agent audit | source: plan-mode audit on feat/encoder-fusion-ablation
Three parallel Explore agents audited PR #110's plan along three angles (code-truth, design-risk, experiment-rigor), in two rounds (pre-checkout via `git show`, then live-tree). Both rounds converged on 5 blockers: (B1) `pose_grad_norm` probe is not implementable as written — `_loss_fn` is in `agent.py:299` not `trainer.py`, and pose is bundled in flat `batch["obs"]` so `jax.grad(...)(pose)` cannot run. (B2) Habitat extrinsics/intrinsics are not in the obs dict at all — `habitat_r2dreamer.py` returns only `{image, reward, is_first, is_last, is_terminal, success, spl}`, so Plücker is broken-by-default. (B3) ReplayBuffer is memory-resident; 22× growth puts the new schema at ~367 GB, mitigation halves to ~283 GB. (B4) `aggregated_tokens` is computed but not returned by `extract()`; the 37×37×1024 shape assumption is unverified. (B5) Four cited paths are wrong (`registry` is in `registries.py:15`; `_loss_fn` is in `agent.py:299`; `eval/` and `slurm/l1/` directories don't exist). Major risks: M1 fixed-JL projection 1024→64 is two orders of magnitude under the JL bound and the matrix isn't seeded/serialized; M2 no seed-variance baseline exists so n=3 may be underpowered; M3 issue #87's "Trivial" fix (scale pose × 100) is missing as a control. Created methods/encoder-fusion-plan-audit.md, updated index.

## [2026-04-30] experiment | L1 act_entropy=3e-2 baseline restore — submitted to unicluster | source: experiment/act-entropy-3e-2
Rerun of the blessed l1-rerun-buffix (wandb y5a0upzd, 75% SR / 0.55 SPL) with `act_entropy=3e-2` made explicit via CLI flag (previous run relied on the hardcoded default; commit 9b5f26f accidentally reverted to DreamerV3 paper default 3e-4). Hypothesis: higher entropy is load-bearing for 75% SR by preventing policy collapse before the world model converges. All other hyperparameters identical to the blessed run. Created experiments/l1-act-entropy-3e-2.md (status: running).

## [2026-04-30] design | Encoder Fusion Plan — pose-aware, geometry-preserving encoder ablation | source: design conversation with Claude
Plan to fix the VGGT encoder confounders #87/#88/#89: current path flattens `world_points (37,37,3)` + `camera_pose (9)` to 4116 floats and projects with a single Dense layer, destroying geometry and drowning pose (0.22% of dims, ~100× smaller magnitude). Four variants share the existing R2Encoder backbone and differ only in pose injection: `vggt_tile` (CoordConv broadcast), `vggt_film` (per-channel γ/β modulation), `vggt_plucker` (per-pixel rays from Habitat extrinsics + intrinsics), `vggt_xattn` (token-level cross-attention with k=8 pose queries on DINOv2 patch tokens). Patch tokens exposed via fixed 1024→64 projection inside the PyTorch extractor (~91k floats/step, 22× current buffer size). Diagnostics: training-time `pose_grad_norm` + eval-time pose-ablation SR drop — the "money diagnostic" for the thesis claim that 3D features only help if pose can influence the latent. Run matrix: 12 H100 runs (4 encoders × 3 seeds), all hyperparameters held to the act_entropy=3e-2 baseline, x-attn deferred as exploratory follow-up. Branch: `feat/encoder-fusion-ablation`. Wrote methods/encoder-fusion-plan.md, updated index.
## [2026-04-27] analyze | R2Dreamer encoder-drift baseline on L1 VGGT (ckpt 300k) — three coherent drift signals on ep7 (success) vs ep1 (near-miss) | source: viz-pair-a/ + 4 commits (9c8057d → d64a647)
First end-to-end run of the encoder-drift visualization pipeline. Picked pair (ep7 success / ep1 near-miss) from a 12-episode instrumented re-roll on the L1 VGGT checkpoint at step 300k; ep1 ends 0.94 m from the goal — **4.7× outside** the 0.2 m success radius (goal_radius=0.2 m per `habitat.py:23`; STOP is a no-op in this env — success is claimed by proximity on any movement action, not by STOP). Most diagnostic failure mode is failure to enter the 0.2 m radius, not STOP-firing. Three convergent metrics show drift: probe_feat RMSE 0.228 m on ep7 vs 0.367 m on ep1 (R² gap 1.06); off-diagonal cosine mean collapses on ep1 (S_feat 0.725→0.771, S_deter 0.784→0.826) while VGGT correctly distinguishes more of ep1 (0.759→0.693); Pearson(S_VGGT, S_feat) inverts (0.380→0.568) — high not because Dreamer is faithful, but because relational rank is preserved while amplitude is compressed. Conclusion: relational topology preserved, absolute geometric resolution lost; deter carries the collapse more than stoch. Three methodological surprises parked as issues — [#101](https://github.com/LSailer/Master-Thesis-3D-VLA/issues/101) eval is non-deterministic across re-runs on H100 (TF32 matmul drift; pair-pick + dumps must come from same run); [#102](https://github.com/LSailer/Master-Thesis-3D-VLA/issues/102) standalone evaluate.py was CNN-only until commit 9c8057d, no VGGT run had ever been evaluated outside the trainer; linear probes near R²<0.3 abort threshold, MLP reserved as Plan B. Page: [methods/r2dreamer-encoder-drift-viz.md](methods/r2dreamer-encoder-drift-viz.md). Interactive 4-panel notebook deferred — analytical narrative locked first.
## [2026-05-03] wire | Autonomous trigger glue: ralph-cron polls `ready+AFK` issues every 15min and dispatches Ralph | source: plan/how-can-i-ask-radiant-cosmos.md
Closed the trigger gap on the existing Ralph + pipeline infra (Ralph + scripts/pipeline/launch.sh were already ~80% built). Added `.github/workflows/ralph-cron.yml` (ubuntu-latest, schedule `*/15 * * * *`) that: skips when Ralph is in_progress/queued, counts open issues with both `ready` and `AFK` labels, and dispatches `ralph.yml` with `max_tasks` (capped at 5). Fixed `.claude/hooks/capture-session-learnings.sh` — was hardcoded to `/Users/luca/Coding/Master-Thesis-3D-VLA` and silently no-op'd on cluster (so weeks of cluster sessions had not been adding to `lessons/`). Now derives `PROJECT_DIR` from `${BASH_SOURCE[0]}` + `pwd -P`, slugger `[/_]→-` for transcript dir lookup. Updated `methods/workflow.md` with current actual flow + marked `/slurm-submit` as not-yet-implemented (today's path: hand-submit via `sbatch modules/r2dreamer/scripts/slurm/<variant>.sbatch`). Five deferred items parked as GitHub issues: pipeline auto-launch on merge, Telegram bot, cross-repo paper-recap bridge to LSailer/master-thesis, finalize-or-remove `/slurm-submit` and `/engineer-team` skills, OpenClaw cleanup. MVP scope per user: minimal — trigger + path fix + docs only; no new infra.

## [2026-04-27] analyze | R2Dreamer encoder-drift baseline on L1 VGGT (ckpt 300k) — three coherent drift signals on ep7 (success) vs ep1 (near-miss) | source: viz-pair-a/ + 4 commits (9c8057d → d64a647)
First end-to-end run of the encoder-drift visualization pipeline. Picked pair (ep7 success / ep1 near-miss) from a 12-episode instrumented re-roll on the L1 VGGT checkpoint at step 300k; ep1 ends 0.94 m from the goal — *inside* the 1.0 m success radius — without firing STOP, which is the most diagnostic failure mode for the actor-vs-encoder question. Three convergent metrics show drift: probe_feat RMSE 0.228 m on ep7 vs 0.367 m on ep1 (R² gap 1.06); off-diagonal cosine mean collapses on ep1 (S_feat 0.725→0.771, S_deter 0.784→0.826) while VGGT correctly distinguishes more of ep1 (0.759→0.693); Pearson(S_VGGT, S_feat) inverts (0.380→0.568) — high not because Dreamer is faithful, but because relational rank is preserved while amplitude is compressed. Conclusion: relational topology preserved, absolute geometric resolution lost; deter carries the collapse more than stoch. Three methodological surprises parked as issues — [#101](https://github.com/LSailer/Master-Thesis-3D-VLA/issues/101) eval is non-deterministic across re-runs on H100 (TF32 matmul drift; pair-pick + dumps must come from same run); [#102](https://github.com/LSailer/Master-Thesis-3D-VLA/issues/102) standalone evaluate.py was CNN-only until commit 9c8057d, no VGGT run had ever been evaluated outside the trainer; linear probes near R²<0.3 abort threshold, MLP reserved as Plan B. Page: [methods/r2dreamer-encoder-drift-viz.md](methods/r2dreamer-encoder-drift-viz.md). Interactive 4-panel notebook deferred — analytical narrative locked first.

## [2026-04-27] decision | OpenClaw shelved — 20-30s/message latency unfit for Slack-from-phone use case | source: install + clean-reinstall + TUI test on uc3n990
Created `methods/openclaw-verdict.md`. Install + Slack OAuth + plugin-registry-bug all worked (8 plugins after 4.24→4.25 upgrade), but `claude -p` cold-spawn model gives ~20s/message structurally (15s pre-LLM overhead, 5s gen). Trade-off: 0€ Max-Sub vs ~3-5€/mo API+persistent-SDK. Updated index. Teardown executed: systemd unit removed, `~/.openclaw` → `~/.openclaw.shelved-20260427-154414`, `npm uninstall -g openclaw` (434 pkgs). Pre-reinstall archive `~/.openclaw.archiv-20260427-143815` retained ≥7d. Slack/HF tokens still need manual rotation (leaked twice in chat).

## [2026-04-27] decision | kl_free per-group — investigated, rejected as recipe-drift | source: smoke audit + 5-agent verification team
First framed as a bug-fix: `_kl_loss` floor on summed-KL across 32 stoch groups (`agent.py:673,679`) gave effective per-group floor 0.031 nats; mini-smoke verified per-group flooring would pin `kl_divergence` at 1.0 and stop posterior collapse (2.00 nats at step 4250 vs 1.40 in pre-patch run). Then a comparison against the canonical R2-Dreamer source at `external/r2dreamer/rssm.py:222-230` (Bansal et al., the actual ported-from base) showed the pre-patch JAX behavior is **port-faithful**: canonical R2-Dreamer also sums-then-floors. The 8/8 match against `danijar/dreamerv3` was a vanilla-DreamerV3 alignment, not a R2-Dreamer alignment. **Decision: revert the patch.** Keep R2-Dreamer-faithful on `main` to preserve comparability with published R2-Dreamer baselines for the thesis (encoder-variable should be the only confound vs prior runs). Patch left unmerged in worktree `worktree-agent-a7b6ae2626d4d3d43` as reference for a future config-toggle ablation. Saves memory `feedback_r2dreamer_canonical_source` so future sessions compare against `external/r2dreamer/` not `danijar/dreamerv3`. Page: [methods/kl-free-per-group-fix.md](methods/kl-free-per-group-fix.md).

## [2026-04-27] verify | loss/dyn ≡ loss/rep is cosmetic (synthetic JAX proof) | source: smoke-audit follow-up
2026-04-27 — verified loss/dyn ≡ loss/rep is cosmetic (synthetic JAX proof) — [methods/dyn-rep-loss-cosmetic-proof.md](methods/dyn-rep-loss-cosmetic-proof.md)

## [2026-04-27] ingest | OpenClaw Install Playbook (BWUniCluster Phase 3) | source: manual playbook
Created methods/openclaw-install.md — BWUniCluster-specific install runbook for OpenClaw with `claude-cli` backend (Claude Max, no Anthropic-API spend) and Slack Socket Mode (`@clusterbot` in `lucasworkspac-6gw5209.slack.com`). Sections: prerequisites with copy-pastable verification commands (node/claude/.env/Slack token curls + Slack user-ID capture for ACL), nvm-based Node 22 install, paste-ready `~/.openclaw/openclaw.json` with `permission-mode acceptEdits` and `cwd` pinned at the repo, tmux+nohup persistence with login-node-roulette workaround (sticky `uc3nNNN.scc.kit.edu` reattach), smoke test against `@clusterbot`, hardening checklist (8 items: ACL, chmod 600, .gitignore for .env + output/openclaw, no token echo in logs, console.anthropic.com zero-spend check), daemon-stability gate that evaluates `/tmp/bwhpc_daemon_test.log` (heartbeat continuity = PASS, gaps/EXIT = FAIL → fall back to `salloc` compute node or local workstation), 8-row BWUniCluster-flavoured troubleshooting table (login-node killer, claude-cli auth drift, PATH-too-narrow for `uv run`, EACCES on PFS quota, etc.). Updated index. Two fuzzy spots flagged inline as verify-don't-assume: exact `~/.nvm/versions/node/v22.x/...` path and whether `nvm` was already installed.

## [2026-04-26] restructure | Output dir migration to LMWiki pattern (Big-Bang PR) | source: /grill-me → recaps/2026-04-26-output-restructure.md
Big-Bang PR (`pipeline/output-restructure`) executes all 13 decisions from the recap. P1 migrated `output/` to the 3-bucket layout (`runs/`, `methods/`, `slurm/`) with `<slug>-<jobid>/` per-run naming and folded parent-level SLURM logs into their run dirs; updated 67 hardcoded paths across scripts/notebooks. P2 added auto-emission of `MANIFEST.json` (git_sha, config snapshot, wandb_id, slurm_id, start/end timestamps) to the trainer entry point. P3 codified the new contract: added "Data layout" section to `CLAUDE.md`, created `docs/wiki/_templates/recap.md`, backfilled YAML frontmatter (`run_path` / `slurm_id` / `wandb_id` / `status`) onto all 7 experiment pages, and extended `slurm-submit` (env var propagation), `reporter` (frontmatter + `_blessed/<alias>` symlink), and `wiki-audit` (frontmatter-driven cross-check) via `/write-a-skill`. `_blessed/<alias>` symlink creation on disk for existing runs deferred to a follow-up issue — frontmatter declares intent.

## [2026-04-26] grill | Auto-Pipeline design — two-skill + bash glue | source: /grill-me → recaps/2026-04-26-auto-pipeline.md
Sixteen design decisions resolved in a single grill session for an automated experiment pipeline (Plan → Engineer → Test → Review). Key calls: (D1) NO new meta-skill — enhance `/grill-me` + create `/engineer-team` + bash glue; (D2) Phase 1+2 share live Opus session, Phase 3+4 run autonomously as SLURM `--dependency=afterok` chain after engineer-team submits via `launch.sh`; (D5) verify and report are `claude -p --model claude-sonnet-4-6` calls (precedent: `modules/vggt/autoresearch/run_autoresearch.sbatch:52`), no Python verify layer; (D9) `/grill-me` becomes universal — every session ends with an eval-pass walking each decision (auto/manual/N/A); (D4) new `docs/wiki/recaps/` folder for grill-session audit trails, log.md indexes both recaps and destillated method/experiment pages. Three mid-session human gates (D10), branch auto-create `pipeline/<name>` (D11), reporter handles HTML+PR (D12), verifier handles results-write+gh-issue (D13). Models: Opus 4.7 for live orchestration, Sonnet 4.6 via Task for sub-agents and via `claude -p` for verify/report. Deliverables next: enhance `/grill-me`, create `/engineer-team`, write 5 pipeline files under `scripts/pipeline/`.

## [2026-04-25] consolidate | Single knowledge base — `.claude/knowledge/` → `docs/wiki/`, CLAUDE.md trimmed to pointer | source: knowledge-base audit
Audit found four parallel knowledge stores (CLAUDE.md, ~/.claude memory, .claude/knowledge/, docs/wiki/). Three are legitimate (different audiences/persistence/visibility); .claude/knowledge/ was orphan scratch with parallel structure to wiki, no skill or hook referenced it. Migrated its single content file (vggt_to_r2dreamer_callchain.md, 121 lines) to methods/vggt-r2dreamer-callchain.md. Removed empty .claude/knowledge/ directory. CLAUDE.md radically trimmed: kept project intro + supervisors + coding principles (rules); replaced Wiki/GitHub-Pages sections with single "Knowledge base" pointer at docs/wiki/index.md plus 3 method-page links; removed Research and Pipeline sections (already implicit in wiki structure). Net: from ~40 lines + 4 sections of redundant content to 28 lines, pointer-shaped. Cross-reference discipline confirmed as the operating model: memory ↔ CLAUDE.md ↔ wiki link rather than duplicate.

## [2026-04-25] codify | Phased Orchestration Pattern | source: post-#85 retrospective
Captured the orchestrator-with-subagents-per-phase recipe that emerged organically during the #85 launcher refactor. Seven operational rules: (1) wiki page is the spec, not the conversation; (2) one subagent per phase, not finer; (3) strict file whitelist beats "surgical changes" guidance — explicit "Do NOT touch" lists prevented 100% of scope creep in Phase 2/3 vs Phase 1; (4) trust-but-verify after every subagent (git status + diff + tests); (5) end-to-end smoke is a REQUIRED gate, not manual aftermath — pytest's sys.path setup hides invocation-path bugs; (6) phase boundary = commit boundary, never auto-commit; (7) test gaps surface as parked GitHub issues. Includes brief template, anti-patterns list, and cost observations (Sonnet subagents ~3-5× cheaper than pure-Opus for same code outcome). Created methods/phase-orchestration.md, updated index.

## [2026-04-25] implement+fix | Launcher refactor #85 — 3 phases + sys.path bootstrap fix | source: orchestrator+subagents
Implementation of the design grilled earlier in the day. Three Sonnet subagents under Opus orchestration: Phase 1 (foundation — Encoder ABC + adapters/ + registries + L1+L2+L3 tests, commit 6d16684), Phase 2 (train+eval functions + 7 per-level shims + 9 sbatch edits + L4 preset matrix tests, commit 395d15f), Phase 3 (parity sub-package + PyTorch archive, commit 7046938). Issues #85 + #52 closed. **Then end-to-end smoke caught a real bug**: the new 7-line shims dropped the `sys.path.insert(0, "../../..")` boilerplate that the old scripts had, causing `python script.py` to fail with `ModuleNotFoundError: modules` at sbatch invocation time. Pytest hid this because it configures sys.path itself. Phase 4 fix added the bootstrap to all 7 train/eval shims (the 2 parity shims from Phase 3 already had it — Phase 3 agent read the original parity scripts and preserved the pattern; Phase 2 agent did not). Smoke L1 cnn (1000 steps, prefill 100) now end-to-end green: curriculum loaded (49998→7499 episodes), trainer.fit() ran at FPS ~7, 2 checkpoints saved, WandB run logged (eg87p9do). Test-gap parked for follow-up: subprocess-mode smoke for shims to catch sys.path-style bugs in pytest.

## [2026-04-25] design | Launcher Refactor — Encoder ABC + per-level shims (#85 + #52) | source: /grill-me
Twelve design branches resolved in a single grill session. Key calls: (1) full absorption of #52's table into the new design (PyTorch scripts go to archive instead of refactor — audit confirmed dormant); (2) sibling `train()` + `evaluate()` functions plus a `parity/` sub-package for the structurally-different parity scripts; (3) `dict[str, Path]` curriculum registry — JSON files at `data/curriculum/level{1-4}_*.json` are source of truth, no dataclass duplicating their content; (4) `Encoder` ABC with `CNNEncoder` and `VGGTEncoder` subclasses (CNN's encoder lives in agent, VGGT's is external — asymmetry hides in subclasses, not in `train()`); (5) per-level shims `(encoder, level)` instead of `--curriculum` flag — sbatch already couples `(encoder, level, output_dir, wandb_name)` and putting that in code prevents wrong-directory drift; (6) L1-L4 test pyramid with curated fixture recycled from `output/vggt/parity/` so L3 checks numerical correctness against #81's parity baseline, not just shapes. Migration: 3 phases (Foundation → Train+Eval → Parity+Archive), one issue with checkboxes. Created `methods/launcher-refactor.md`. Updated #85 with Final Agreed Design. Commented on #52 (subsumed). Updated index.md.

## [2026-04-24] perf | VGGT JAX camera_head jit (closes #81) | source: /grill-me + direct implementation
Camera_head was the last eager phase in the JAX extractor: 1187 ms/frame (95% of extract). Mirrored PR #80's padded 3-tuple + `jax.jit` pattern: single compiled graph (no `is_first_frame` static arg needed — `valid_len=0` covers frame 0 in the padded path), `_CAM_MAX = max_camera_frames × num_iterations = 4096` slots, no eviction (camera cache holds the whole episode). Added `test_jax_parity.py::TestLevel3CameraHeadPaddedParity` locking padded-vs-legacy bit parity (atol 1e-5) and `test_jax_integration.py::test_camera_cache_overflow_raises` for the overflow guard (raises `RuntimeError` instead of silent corruption via `dynamic_update_slice` clamping). Results (bf16, H100, n=10): camera_head 1187 → 4.33 ms (274×); extract total 1247 → 64 ms; `bench_fast` JAX median 1763 → 137 ms vs PT 72 ms → speedup 0.041 → **0.524×** (issue exit gate 0.5× met). Parity bit-identical to pre-change on L1 episode 23795 dump. Bug caught mid-session: `profile_streaming.py` was calling `ext._camera_head.apply` eagerly, bypassing the new jit wrapper — first post-change profile showed no improvement until the bench was fixed to route through `ext._camera_head_apply`. Commits: 90e123d (code+tests+dump script) + 648abff (tsv hash fix). Created `methods/vggt-jax-streaming.md`. Phase 2 (shared padded_cache helper extraction) parked for a future session.

## [2026-04-24] refactor | dreamerv3 archival + architecture audit | source: /grill-me + /improve-codebase-architecture
Cleanup session. Archived `modules/dreamerv3/` as-is to `archiv/dreamerv3-20260424/` via `git mv`; peeled `configs.py`, `optim.py`, `replay_buffer.py` + `test_replay_buffer.py` to `modules/shared/`; updated 17 import sites; `pyproject.toml` testpaths updated. 90/90 tests pass (76 shared+r2dreamer, 14 envs). Output dir 37 GB → 21 GB (deleted 6 stale run dirs + 3 checkpoint dirs from `r2dreamer-habitat-baseline/`, kept metrics.csv + slurm logs). Architecture audit produced 3 deepening RFCs with parallel sub-agent design rounds: #84 (deepen `HabitatObjectNavEnv` — owned types + thin NavBackend port + stub), #85 (deepen training entrypoint — two-axis registry + curriculum dataclass, supersedes #52), #83 parked (base `WorldModelConfig` consolidation). Perf brainstorm parked as #82 for next session. Closed #69. Grill surfaced that L1-VGGT job 3957756 already reached ~47-63% SR (resume artifact explains dip) — below 2D L1 (75%) but not a blocker since curriculum-level comparison (L2/L3/L4) is the real plot. Research direction locked: primary plot = 2D-CNN vs 3D-VGGT curriculum SR; encoder = raw VGGT first, semantic DPT head as stretch; UNITE dropped.

## [2026-04-20] update | torch.compile spike on VGGT | source: /plan Slice 3 follow-up (PRD #74)
`VGGTFeatureExtractor(compile=True)` wraps aggregator/camera_head with dynamic=True, point_head static. Measured 11% p50 reduction on vggt_forward (168.3 → 149.8 ms), p95 also 11%, more consistent frame-to-frame. Per-step p50: 190.7 → 171.0 ms. Shipped as opt-in flag + `--compile` CLI. Wiki section appended to methods/l4-profiling.md. JSON: output/profiling/vggt_vs_cnn_20260420_120455.json.

## [2026-04-20] ingest | L4 Pipeline Profiling (VGGT vs CNN) | source: /plan Slice 3 (PRD #74)
Full 2000/2000 diagnostic on H100. VGGT forward = 168.3 ms p50 — 90% of the VGGT-vs-CNN slowdown. PyTorch↔JAX boundary (vggt_wrapper + jax_upload) = 0.44 ms — disproves the original #72 motivation. KV-cache reset contract verified (9 resets, 9 boundaries). Recommendation: repurpose #72 away from JAX port toward torch.compile / frame-skip / distillation. Created methods/l4-profiling.md. JSON: output/profiling/vggt_vs_cnn_20260420_{102405,103840}.json.

## [2026-04-16] ingest | Curriculum Scaling (L1-rerun, L2, L3) | source: /reporter
Updated experiments/l1-rerun-buffix.md, experiments/l2-1house-6goals.md, experiments/l3-10houses-chair.md with completed status. Semantic floor plan data collected for fK2vEV32Lag (L2 scene). Plot scripts and combined slide deck created.

## [2026-04-16] ingest | L1 Rerun, L2, L3 Experiments | source: /reporter (paused)
Three curriculum experiments completed (SLURM 3957651, 3957713, 3957714). L1 rerun: 75% SR (buffer fix +8pp). L2: 36% avg SR with goal hierarchy (plant 66% > tv_monitor 3%) — Geo/Euc ratio is strongest predictor. L3: 32% SR across 10 houses. Created experiments/l1-rerun-buffix.md, l2-1house-6goals.md, l3-10houses-chair.md. Reporter paused pending semantic floor plan rendering on cluster (issue #71). Updated index.

## [2026-04-15] ingest | Training Orchestration | source: /engineer
Implemented RFC #68: unified ReplayBuffer+VGGTReplayBuffer with BufferConfig, created Trainer module (convert_batch, checkpoint with ema_state fix, ObsAdapter, habitat_defaults). Rewrote 3 training scripts (-62% lines). Created methods/training-orchestration.md. Updated index.

## [2026-04-15] ingest | World Model Training Loop | source: /plan interview
Explained how 64-step replay windows, 15-step imagination horizon, and full-length acting episodes fit together. Created methods/world-model-training-loop.md. Updated index.

## [2026-04-15] ingest | L1 Baseline 2.4M | source: /reporter
R2-Dreamer on L1 curriculum (wandb krokhgwi, SLURM 3923812). 67% SR, 0.49 SPL — 17x above random. World model learns but overfits (val dyn 17→42). Created experiments/l1-baseline-2.4m.md. Updated index. Generated 5 plot figures.

## [2026-04-13] ingest | Random Baseline L1 | source: /reporter
Random agent on L1 curriculum (834 eval episodes, 1 house, chair only). 3.84% SR, 0.023 SPL, -4.40 mean reward. Created experiments/random-baseline-l1.md. Updated index. Generated 4 plot figures.

## [2026-04-13] ingest | Baseline 2.4M All Scenes | source: /reporter
Analyzed r2dreamer-baseline-2.4M-3907457 (wandb qwdqowxq). 4871 episodes, 2.36% SR (random-level), no learning. Created experiments/baseline-2.4m-all-scenes.md. Updated index. Generated 3 plot figures.

## [2026-04-13] ingest | 2026-03-03 Braun Meeting | source: manual migration
Migrated from docs/notes/2026-03-03-meeting.md. Created meetings/2026-03-03-braun.md. Updated index.

## [2026-04-13] ingest | Claude Code Workflow | source: manual
Created methods/workflow.md documenting full pipeline, skill inventory, TDD rules, wiki conventions. Updated index.

## [2026-04-25] ingest | Cross-Correlation Matrix concept | source: brainstorm session on wandb run 87u0l6dy
Created methods/cross-correlation-matrix.md. Short concept page: what `c[i,j]` is, why diagonal target = 1 and off-diagonal target = 0, and that it's used as a distillation regularizer here (RSSM↔VGGT) rather than as a contrastive loss between augmented views. Updated index.

## [2026-04-25] update | L4 profiling re-verification + JAX comparison | source: profile_training.py reruns on main
Re-ran VGGT no-compile / VGGT compile / CNN profiles under current main (post launcher Phase 1–3 refactor, post `act_entropy` fix #93). All VGGT phases reproduce 2026-04-20 within noise; KV-cache audit 9=9 unchanged. Added JAX-vs-PyTorch apples-to-apples row from `modules/vggt/autoresearch/bench_fast.py`: JAX promoted commit `90e123d` is 1.9× slower than PT-fp32 baseline. Decisions: #88 closed won't-fix (wrapper+upload = 0.47% of bottleneck), JAX port reframed as uniformity-not-speed track (#98), frame-skip filed as separate throughput issue (#97). CNN `wm_training` +12% regression flagged for follow-up.

## [2026-05-08] ingest | R2-Dreamer Parity A/B on Crafter | source: launch-time
Created experiments/r2dreamer-parity-crafter.md for the 1M-step parity smoke A/B (1 seed each) between modules/r2dreamer (JAX) and external/r2dreamer (PyTorch ref). New isolated W&B project `r2dreamer-parity-crafter`. Wrapper `modules/r2dreamer/launch/run_external_crafter.py` mirrors external's TensorBoard events to W&B via `wandb.init(sync_tensorboard=True)` — no edits to vendored external/. Dedicated `external/r2dreamer/.venv` sidesteps numpy/torch pin conflicts with the parent uv project. Slurm: ab_internal_crafter_s0.sbatch + ab_external_crafter_s0.sbatch. Updated index.
