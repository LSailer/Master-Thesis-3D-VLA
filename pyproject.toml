[project]
name = "master-thesis-3d-vla"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
    # VGGT deps
    "torch>=2.7",
    "torchvision>=0.22",
    "numpy>=1.26",
    "Pillow",
    "huggingface_hub",
    "einops",
    "safetensors",
    # notebook
    "ipykernel",
    "matplotlib",
    "manim>=0.18.1",
    # ALFRED / AI2-THOR
    "ai2thor>=5.0.0",
    # dev
    "pytest",
]

[tool.uv]
python-preference = "only-managed"
